name: Deploy NEWEeRAS Platform

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Build application
      run: npm run build
      env:
        NODE_ENV: production

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./out
Click "Commit changes"
Step 3: Delete the simple index.html
Click on index.html
Click trash icon (delete)
Click "Commit changes"
Now your FULL React platform will build properly! Check Actions tab in 5-10 minutes for green checkmarks. 🚀

You'll get your premium, one-of-a-kind platform exactly as designed!



take away your suggestions i cant copy the code its in my way

Office Agent
Office Agent
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  distDir: 'out',
  assetPrefix: process.env.NODE_ENV === 'production' ? '/neweeras-platform' : '',
  basePath: process.env.NODE_ENV === 'production' ? '/neweeras-platform' : '',
}

module.exports = nextConfig



